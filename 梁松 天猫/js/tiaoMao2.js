 $(document).ready(function () {     var div = $('.datu')     var imgs = $('.datu>ul>li')     var width = div.width();     console.log(width);     var lis = $('.dian1>li');     var t = setInterval(move, 1500)     var now = 0;     var nxet = 0;     var flag = true;     function move() {         next = now + 1;         if (next >= imgs.length) {             next = 0;         }         imgs.eq(next).css({left: width}).end().eq(now).animate({left: -width}).end().eq(next).animate({left: 0});         lis.eq(now).removeClass('active').end().eq(next).addClass('active');         now = next;     }     $(document.body).on('mousemove', function () {         clearInterval(t);     })     $(document.body).on('mouseout', function () {         t = setInterval(move, 1500);     })     //lis.each(function (index, value) {     //    // consolele.log(this)     //    $(this).click(function () {     //        if ($(this).index() > now) {     //            imgs.eq(index).css({left: width});     //            imgs.eq(now).animate({left: -width});     //            imgs.eq(index).animate({left: 0});     //            lis.eq(now).removeClass('active')     //            lis.eq(index).addClass('active');     //            now = index;     //        }     //        if ($('this').index() < now) {     //            imgs.eq(index).css({left:- width});     //            imgs.eq(now).animate({left: width});     //            imgs.eq(index).animate({left: 0});     //            lis.eq(now).removeClass('active')     //            lis.eq(index).addClass('active');     //            now = index;     //        }     //    })     //}) // $(document.body).on('mousemove', function () { //     clearInterval(t); // }) // $(document.body).on('mouseout', function () { //     t = setInterval(move, 2000); // }) // lis.each(function (index, value) { // // }) })